packages:
  - apps/*
  - packages/*

# pnpm 11 build-script policy. Only esbuild (used by vitest) may run scripts;
# @reown/appkit's postinstall is not needed for anything we use.
allowBuilds:
  esbuild: true
  '@reown/appkit': false
